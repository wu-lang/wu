return (
function()
  local ___type = setmetatable({}, {__index=_ENV})
  -- type `point` is defined here
  local player_pos = 
  local foo = function(bar)
    return
  end
  foo(player_pos)
  return ___type
end)()